package thelarsinator.extfar.tileentity;

import net.minecraft.client.renderer.tileentity.TileEntityRenderer;
import net.minecraftforge.api.distmarker.Dist;
import net.minecraftforge.api.distmarker.OnlyIn;

@OnlyIn(Dist.CLIENT)
public class SprayerRenderer extends TileEntityRenderer<SprayerTileEntity> {
    @SuppressWarnings("deprecation")
    @Override
    public void render(SprayerTileEntity tileEntityIn, double x, double y, double z, float partialTicks, int destroyStage){

    }
}
